#include"Run.h"
#include<iostream>



int main() {

	 //x,y,radomNum  all is unsigned int
	//creatMap cmp(20, 30,40);
	//cmp.Go();

	//for (int i = 0; i != 1000; ++i) {
	//	if (i % 5 == 0 && i % 10 == 0 && i % 15 == 0 && i % 20 == 0 && i % 25 == 0) {
	//		std::cout << i << std::endl;
	//	}
	//}

	return 0;
}